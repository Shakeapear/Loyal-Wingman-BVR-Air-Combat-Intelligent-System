# 面向忠诚僚机超视距空战的知识数据双驱动无人智能及自迭代技术研究

## 详细研究计划与实施方案

**项目负责人：** 李欣洋  
**指导教师：** 曾俊杰  
**起止时间：** 2026年6月1日 — 2027年6月1日  
**所属单位：** 国防科技大学 军政基础教育学院  
**进度状态：** 全局进度与完成状况以根目录《项目状态与进度总览.md》为准（最近更新 2026-09-01）

---

# 第一篇 总体研究方案

## 一、项目背景与总体目标

### 1.1 项目背景

以新一代有人隐身战斗机指挥歼-6、歼-7等老旧机型改装的无人僚机遂行超视距（BVR）空战为核心场景（老旧平台性能通过JSBSim现有机型施加约束来模拟，不自行建模），针对现有无人僚机技术中**DRL决策黑箱、人机信任缺失、多任务适配困难、部署后无法持续迭代**四大核心痛点，开展面向忠诚僚机超视距空战的知识数据双驱动无人智能及自迭代技术研究。

### 1.2 总体目标

开发一套**可自迭代的行为树与智能体嵌套式混合智能决策算法**，形成自主可控、可持续优化、适配老旧平台的无人僚机智能决策方案（老旧平台通过JSBSim现有机型施加性能约束来模拟，不自行建模）。具体包括：

1. 基于JSBSim构建适配老旧平台的高保真超视距空战仿真环境1套（选用F-104/F-4N等现有机型 + 性能约束）
2. 行为树与智能体嵌套式混合智能决策框架1套（含三层调度行为树、黑板系统、多任务专精DRL智能体、人机交互界面、OpenClaw自迭代模块）
3. 项目研究报告1份

### 1.3 技术架构总览

```
┌─────────────────────────────────────────────────────────┐
│                    验证层（第五阶段）                      │
│      操控功能验证 │ 自迭代功能验证 │ 效能评估              │
├─────────────────────────────────────────────────────────┤
│                    集成层（第四阶段）                      │
│  三层调度行为树 │ 黑板系统 │ OpenClaw闭环迭代机制         │
├─────────────────────────────────────────────────────────┤
│                    智能层（第三阶段）                      │
│  JSRL双策略课程式训练 │ 多任务专精DRL智能体训练           │
├─────────────────────────────────────────────────────────┤
│                    规则层（第二阶段）                      │
│  LLM行为树生成器-测试器 │ 二级优先级引导策略行为树         │
├─────────────────────────────────────────────────────────┤
│                    仿真层（第一阶段）                      │
│  JSBSim六自由度仿真环境 │ 适配机型选型 │ 标准化接口        │
└─────────────────────────────────────────────────────────┘
```

---

## 二、现有基础评估

### 2.1 已具备条件

| 类别 | 具体内容 | 完备程度 | 说明 |
|------|---------|---------|------|
| 仿真引擎 | JSBSim v1.2.4 完整安装包 | ★★★★★ | 含60型飞行器模型、63个仿真脚本、86个发动机定义、30个可复用系统组件、MATLAB接口、Aeromatic++模型生成器 |
| 飞行器模型 | F-16、F-15、F-22等战斗机模板；F-104（J79单发轻型机）、F-4N（J79双发重型机）等老旧代际机型 | ★★★★★ | 无需自行建模，可直接选用JSBSim现有机型并施加性能约束，F-104/F-4N是最适配的僚机平台候选 |
| 发动机模型 | F100-PW-229（F-15/F-16用）、J79-GE-11A（F-104/F-4用）、J85-GE-5（T-38用）等 | ★★★★☆ | J79系列与老旧涡喷发动机技术同源，参数可直接使用 |
| 武器/传感器模型 | 中距弹+雷达模型（已完成，2026-08） | ★★★★☆ | 已实现于 `多任务智能体/common/missile_model.py`、`radar_model.py`：3DOF双推力中距弹（TPN+中制导交接+APN、LAE/NEZ解算）、火控雷达/RWR/MAWS，24项单元测试通过 |
| 开发工具链 | Python/C++编译环境、PyTorch、JSBSim.lib | ★★★★☆ | 团队已掌握，可支撑开发 |
| 前期预研（现状） | F-104选型+性能约束、BVRCombatEnv、5技能任务智能体验证 | ★★★★★ | 已完成：check_env通过、4实例并行1611 steps/s、导弹40-55km迎头命中可复现、5个技能智能体验证性运行通过（2026-08-17） |

### 2.2 待建设内容（状态截至 2026-09-01，详见《项目状态与进度总览.md》）

| 类别 | 待建设内容 | 优先级 | 预计工作量 | 状态 |
|------|-----------|--------|-----------|------|
| 适配机型选型 | F-104/F-4N等现有机型性能对比、约束参数配置 | 高 | 2周 | ✅ 已完成（F-104主选+F-4N备选，约束在环境边界判定中生效） |
| 武器模型 | 中距弹弹道模型、导弹攻击区/不可逃逸区、雷达探测模型 | 高 | 4周 | ✅ 已完成（`common/missile_model.py`、`radar_model.py`，24项单测+攻击区图） |
| 环境接口 | 标准化Gym接口（动作空间、状态空间、奖励函数） | 高 | 3周 | ✅ 已完成（BVRCombatEnv+bridge，check_env通过、4实例1611 steps/s） |
| 可视化 | 接入FlightGear或自定义可视化方案 | 中 | 3周 | ⬜ 未开始（逾期，仍需完成；对应实施计划书步骤2.4） |
| LLM行为树生成 | Coder-Tester协同进化框架 | 高 | 6周 | ⬜ 未开始（当前窗口，9.15里程碑；bt_validator Python重写已备） |
| JSRL训练框架 | 行为树引导策略 + 课程式训练 | 高 | 8周 | ⬜ 未开始（阶段三，11月起） |
| 多任务DRL智能体 | PPO算法实现、多任务训练 | 高 | 8周 | 🟡 部分（5技能任务智能体验证通过；任务级打击/巡逻/防御待阶段三） |
| 三层调度行为树 | 指令仲裁、安全兜底、智能体调度 | 最高 | 6周 | ⬜ 未开始 |
| 黑板系统 | 中心化信息存储与交换枢纽 | 高 | 4周 | ⬜ 未开始 |
| OpenClaw集成 | 反馈收集、信号转化、策略微调 | 高 | 4周 | ⬜ 未开始 |
| 效能评估 | 指标体系、对比实验、统计分析 | 中 | 4周 | ⬜ 未开始 |

---

# 第二篇 分阶段详细实施方案

## 第一阶段：高保真超视距空战仿真环境搭建

**时间段：** 2026年6月 — 2026年7月（8周）  
**负责人：** 唐煜皓（主责）、李欣洋（协责）  
**经费分配：** 算力租赁 ¥4000、材料费 ¥2000

---

### 任务1.1：适配现有机型选型与性能约束配置（第1-2周）

#### 1.1.1 机型选型对比

**选型策略：** 本项目不从零建模老旧机型，而是直接从JSBSim现有60型飞行器模型库中选取性能参数与老旧平台相近的成熟机型，通过施加性能约束来模拟老旧平台限制。

**候选机型评估（均来自JSBSim现有模型库）：**

| 机型 | JSBSim目录 | 年代 | 发动机 | 最大马赫 | 推重比(军推) | 适配度 | 说明 |
|------|-----------|------|--------|:---:|:---:|:---:|------|
| **F-104 Starfighter** | `aircraft/f104/` | 1958 | J79-GE-11A ×1 | M2.2 | ~0.54 | ★★★★★ | 单发轻型截击机，J79与涡喷-6同代技术，已内置完整XML模型 |
| **F-4N Phantom II** | `aircraft/F4N/` | 1960 | J79-GE-8B ×2 | M2.2 | ~0.60 | ★★★★☆ | 双发重型，可模拟更大平台，J79同源 |
| F-80C Shooting Star | `aircraft/F80C/` | 1948 | J33-A-35 ×1 | M0.76 | ~0.37 | ★★★☆☆ | 亚音速，性能偏低 |
| T-38 Talon | `aircraft/T38/` | 1961 | J85-GE-5 ×2 | M1.3 | ~0.50 | ★★★☆☆ | 超音速教练机，体型偏小 |

**最终选型：** 推荐选用 **F-104 Starfighter（主选平台）** 和 **F-4N Phantom II（对比/备选平台）**。

**选型理由：**
1. F-104 为单发轻型截击机，J79涡喷发动机与老旧涡喷发动机处于同一技术代差，推重比和机动性可较好地代表限制平台能力
2. F-4N 为双发重型机，可模拟不同量级的改装平台，丰富实验对比维度
3. 二者均在 JSBSim `aircraft/` 目录中有完整的 XML 配置（含气动、发动机、飞控、起落架等子系统），无需额外编写建模文件
4. JSBSim 已内置配套的仿真脚本（`f16_test.xml` 风格），可直接复用并适配

#### 1.1.2 性能约束配置

在选用现有机型的基础上，对关键性能参数施加约束以模拟老旧平台的结构老化和改装限制：

| 约束项 | F-104 原始值 | 约束后值 | 约束依据 |
|--------|:----------:|:------:|---------|
| 最大正过载 | +7.3G | **+5G** | 老旧机体结构降级，限制机动强度 |
| 最大负过载 | -3G | **-1.5G** | 对称限制 |
| 最大马赫数 | M2.2 | **M1.6** | 模拟发动机老化和外挂阻力增大 |
| 最大升限 | 18000m | **14000m** | 性能退化 |
| 武器挂载 | 原始武器配置 | **2×中距弹** | 限制挂载能力，模拟改装平台武器接口限制 |
| 雷达探测距离 | 原始雷达参数 | **≤80km**（RCS=5m²） | 模拟老式雷达性能 |

**实现方式：**
- 约束逻辑实现在 Python 层的 `SafetyGuard` / `BoundaryConstraint` 模块中，不修改 JSBSim 原始 XML 文件
- 当仿真环境输出超出约束值时，自动裁剪到约束边界
- 约束触发时记录在黑板系统的安全校验日志中

**验收标准：**
- 选型机型可被JSBSim.exe加载运行不报错
- 性能约束在仿真运行中实际生效（日志中可观察到约束触发记录）
- 飞行动力学响应物理合理

---

### 任务1.2：战场要素建模（第3-4周）

#### 1.2.1 导弹模型

**需实现的核心模型：**

| 模型组件 | 功能描述 | 实现方式 |
|---------|---------|---------|
| 中距弹运动学 | 三自由度质点动力学（最大过载限制、发动机工作时间、最大速度） | Python数值积分 |
| 导弹攻击区（LAE/AE） | 计算当前态势下的最大/最小/不可逃逸发射距离 | 基于能量-运动学快速解算 |
| 命中判定 | 脱靶量阈值判定（圆形/球形杀伤区） | Python逻辑判断 |
| 箔条/红外干扰弹 | 简化概率模型（干扰成功率随距离/角度变化） | Python概率模型 |

**导弹参数参考（公开数据）：**
- AIM-120C AMRAAM级别中距弹：最大射程~100km，主动雷达导引头作用距离~20km
- PL-12/SD-10级别中距弹：最大射程~70-100km

**验收标准：**
- 攻击区计算结果与公开文献曲线趋势一致
- 不可逃逸区随高度、速度、双方态势动态变化合理
- 导弹飞行轨迹物理合理

#### 1.2.2 雷达与传感器模型

| 模型组件 | 功能描述 |
|---------|---------|
| 机载火控雷达 | 最大探测距离（RCS相关）、方位/俯仰扫描范围、跟踪目标数 |
| 雷达告警接收机（RWR） | 敌方雷达锁定告警 |
| 导弹逼近告警（MAWS） | 来袭导弹检测 |

---

### 任务1.3：交互接口与可视化（第5-7周）

#### 1.3.1 标准化Gym接口

参考OpenAI Gym规范，设计空战环境接口：

```python
class BVRCombatEnv(gym.Env):
    """
    超视距空战强化学习环境
    
    动作空间 (Action Space):
        - 连续控制: [油门(0-1), 滚转角(-1~1), 俯仰角(-1~1), 偏航角(-1~1)]
        - 武器控制: 发射导弹(离散0/1), 切换武器(离散), 开启ECM(离散)
    
    状态空间 (State Space):
        - 本机状态: [x,y,z, vx,vy,vz, roll,pitch,yaw, Alpha,Beta, 油量, 武器数量]
        - 目标信息: [相对方位角, 相对俯仰角, 相对距离, 接近速度, 目标速度, 目标高度]
        - 威胁信息: [是否有导弹来袭, 导弹距离, 导弹方位]
        - 任务标签: one-hot编码 [打击/巡逻/防御]
    
    奖励函数:
        基础奖励: 命中目标 +100, 被命中 -100, 坠毁 -50
        态势奖励: 进入攻击区 +5/step, 保持在有利态势 +2/step
        安全惩罚: 超出飞行包线 -10, 进入敌方攻击区 -3/step
    """
```

#### 1.3.2 JSBSim-Gym桥接层

```
Python (RL Training)  <──>  JSBSimInterface <──> JSBSim C++ Engine
        │                        │
    Gym Interface           JSBSim.lib
        │                        │
    PyTorch Model           XML Config
```

**实施步骤：**
1. 封装`JSBSimGymBridge`类，管理JSBSim实例的生命周期
2. 实现`step(action) → (obs, reward, done, info)`接口
3. 实现`reset()`接口，支持随机初始态势
4. 实现多实例并行（支持向量化环境VecEnv）

#### 1.3.3 可视化方案

**方案A（推荐）：** 基于Matplotlib/Folium的2D战术显示
- 显示双方轨迹、导弹轨迹、攻击区范围
- 实时更新态势面板
- 录制回放功能（保存轨迹CSV + 动画脚本）

**方案B（高级）：** 接入FlightGear可视化引擎
- 利用JSBSim内置的FlightGear输出协议
- 提供3D视景

**验收标准：**
- 可视化能清晰展示空战态势
- 支持事后复盘回放
- 至少实现方案A

---

### 任务1.4：环境验证与精度校准（第7-8周）

**验证项目：**

| 验证项 | 方法 | 验收标准 |
|--------|------|---------|
| 飞行性能验证 | 对比JSBSim输出与公开手册数据（最大速度、升限、爬升率等） | 误差 < 15% |
| 导弹模型验证 | 对比攻击区图与公开文献 | 趋势一致 |
| 接口功能验证 | 单元测试覆盖所有Gym接口 | 通过率 100% |
| 环境稳定性 | 连续运行1000 episode无崩溃 | 无崩溃 |
| 多实例并行 | 4实例并行运行，吞吐量测试 | ≥ 1000 steps/s |

---

## 第二阶段：二级引导策略行为树设计与实现

**时间段：** 2026年8月 — 2026年10月（12周）  
**负责人：** 李欣洋（主责）、焦阳（协责）  
**经费分配：** 文献检索 ¥1000、算力 ¥2000

---

### 任务2.1：LLM行为树生成器-测试器框架（第1-5周）

#### 2.1.1 框架总体设计

```
┌──────────────────────────────────────────────────────────┐
│                LLM Behavior Tree Generator                │
├──────────────────────────────────────────────────────────┤
│  ┌─────────────┐    ┌─────────────┐    ┌──────────────┐ │
│  │   Coder     │───▶│  Executor   │───▶│   Tester     │ │
│  │  (LLM生成)  │    │ (JSBSim执行) │    │  (LLM验证)   │ │
│  └─────────────┘    └─────────────┘    └──────────────┘ │
│         ▲                                      │         │
│         └────────── Feedback Loop ─────────────┘         │
└──────────────────────────────────────────────────────────┘
```

#### 2.1.2 LLM Prompt工程

**Coder Prompt模板（行为树生成器）：**

```
你是超视距空战行为树专家。请根据以下任务描述，生成一个行为树（Behavior Tree）的XML/伪代码描述。

## 任务描述
{mission_description}

## 可用节点类型
- Selector（选择节点）：依次执行子节点，任一成功即返回成功
- Sequence（序列节点）：依次执行子节点，任一失败即返回失败
- Parallel（并行节点）：同时执行所有子节点
- Condition（条件节点）：判断某个条件是否满足
- Action（动作节点）：执行具体动作

## 可用动作
- navigate_to_waypoint(altitude, speed, heading)
- search_target(radar_mode)
- lock_target()
- fire_missile()
- evade_missile(maneuver_type)
- maintain_formation(offset_x, offset_y, offset_z)
- return_to_base()

## 可用条件
- has_target_detected()
- is_in_launch_zone()
- is_missile_incoming()
- has_weapon_remaining()
- is_fuel_low()
- has_human_command()

## 输出格式
请以JSON格式输出，包含nodes（节点列表）和edges（连接关系）。
```

**Tester Prompt模板（测试用例生成器）：**

```
请为以下行为树生成5组测试用例，每组包含初始态势参数和预期行为描述。

行为树描述：
{bt_description}

测试用例格式：
{
  "test_cases": [
    {
      "initial_state": {
        "ownship": {"altitude": xxx, "speed": xxx, ...},
        "target": {"altitude": xxx, "speed": xxx, "relative_bearing": xxx, ...}
      },
      "expected_behavior": "描述"，
      "success_criteria": {"condition": "xx", "threshold": xx}
    }
  ]
}
```

#### 2.1.3 迭代验证流程

```
Round 1: Coder生成 BT_v1 → JSBSim执行 → Tester生成测试用例 → 评估
Round 2: Coder修正 BT_v2 → JSBSim执行 → Tester验证 → 评估
...
Round N: 达到收敛标准 → 输出最终行为树
```

**收敛判断标准：**
- 测试用例通过率 > 90%
- 行为树覆盖率（超视距空战OODA环关键节点覆盖度）> 95%
- 安全性违规次数 = 0（在测试用例中）

---

### 任务2.2：二级优先级引导策略行为树构建（第6-10周）

#### 2.2.1 第一优先级：边界约束子树

**硬编码安全边界（确定性规则）：**

| 约束项 | 条件 | 响应动作 |
|--------|------|---------|
| 最大过载 | \|G_load\| > +7G / <-3G | 限制操纵输入变化率 |
| 最小航速 | IAS < 1.2×StallSpeed | 推油门、降低迎角 |
| 最大速度 | Mach > M_max | 收油门、打开减速板 |
| 最小高度 | AGL < 安全高度阈值 | 拉起至安全高度 |
| 禁飞区 | 进入禁飞区边界 | 最近方向脱离 |
| 燃油耗尽 | Fuel < 返航所需最低燃油 | 强制返航 |

**实现方式：** 纯Python条件判断，优先级最高，不可被跳过

#### 2.2.2 第二优先级：战术任务执行子树

**覆盖超视距空战OODA环的完整节点链：**

```
BVR Combat OODA Loop Behavior Tree:

[Selector: BVR_Mission]
├── [Sequence: Human_Command_Response]    ← 真人指令响应
│   ├── HasHumanCommand?
│   └── ExecuteCommand
│
├── [Sequence: Self_Defense]              ← 应急自保
│   ├── MissileIncoming?
│   └── EvadeManeuver
│
├── [Sequence: BVR_Engagement]            ← 超视距交战
│   ├── [Selector: Detection]              ← 观察 (Observe)
│   │   ├── TargetDetected?  → Success
│   │   └── SearchTarget      → Running
│   ├── [Sequence: Orientation]           ← 判断 (Orient)
│   │   ├── AssessThreat
│   │   └── AssessLaunchZone
│   ├── [Sequence: Decision]              ← 决策 (Decide)
│   │   ├── [Selector: TacticalChoice]
│   │   │   ├── [Sequence: Offensive]     ← 进攻可行
│   │   │   │   ├── InLaunchZone?
│   │   │   │   └── SetOffensivePosture
│   │   │   └── [Sequence: Defensive]     ← 需防守
│   │   │       ├── InEnemyLaunchZone?
│   │   │       └── SetDefensivePosture
│   │   └── SelectWeaponType
│   └── [Sequence: Action]               ← 行动 (Act)
│       ├── ManeuverToLaunchZone
│       └── [Selector: FireDecision]
│           ├── InNoEscapeZone? → FireMissile
│           └── InLaunchZone? → ConsiderFire
│
└── [Sequence: Default_Patrol]           ← 默认巡逻
    ├── NavigateToPatrolPoint
    └── ExecuteSearchPattern
```

#### 2.2.3 JSRL引导接口设计

在行为树的每个动作节点中预留引导接口：

```python
class GuideInterface:
    """JSRL引导接口 - 嵌入行为树动作节点"""
    
    def __init__(self):
        self.guide_active = True        # 引导策略是否激活
        self.current_guide_step = 0     # 当前引导步数
        self.max_guide_steps = None     # 本episode最大引导步数
        
    def get_action(self, obs, guide_policy):
        """获取引导策略推荐的动作"""
        if self.guide_active and self.current_guide_step < self.max_guide_steps:
            action = guide_policy.get_action(obs)  # 行为树决策
            self.current_guide_step += 1
            return action, True  # (action, is_guided)
        else:
            return None, False   # 交由DRL探索策略
    
    def update_guide_steps(self, new_max_steps):
        """课程式更新引导步数"""
        self.max_guide_steps = new_max_steps
```

---

### 任务2.3：行为树仿真验证（第11-12周）

**验证方法：**
- 在JSBSim仿真环境中运行行为树，测试20组典型初始态势
- 记录关键指标：存活率、命中率、误发射率、安全违规次数
- 对比：LLM生成行为树 vs. 人工手写行为树

**验收标准：**
- 存活率 > 80%（在均匀采样态势下）
- 安全违规次数 = 0
- 行为树节点执行逻辑可完整追溯

---

## 第三阶段：行为树引导的高效DRL智能体研究

**时间段：** 2026年10月 — 2027年1月（14周）  
**负责人：** 李欣洋（主责）、焦阳（协责）  
**经费分配：** 算力租赁 ¥4000、材料费 ¥2000

---

### 任务3.1：JSRL双策略课程式训练框架实现（第1-5周）

#### 3.1.1 JSRL算法原理

```
Algorithm: Jump-Start RL with Behavior Tree Guide Policy

Input:
  π_guide: 行为树引导策略 (from Phase 2)
  π_explore: DRL探索策略 (PPO)
  H_0: 初始引导步数 = episode_length
  K: 训练总episode数

Output:
  π_explore*: 训练完成的DRL策略

for episode = 1 to K:
    state = env.reset()
    h = schedule(episode)  # 课程式减小引导步数
    done = False
    
    for t = 0 to episode_length:
        if t < h:
            action = π_guide(state)       # 行为树引导
        else:
            action = π_explore(state)     # DRL探索
            
        next_state, reward, done = env.step(action)
        
        if t < h:
            # 引导阶段收集数据（可选：Behavior Cloning损失）
            guide_buffer.append(state, action, π_explore.log_prob(state, action))
        else:
            # 探索阶段正常PPO训练
            ppo_buffer.append(state, action, reward, ...)
        
        state = next_state
        if done: break
    
    # PPO更新
    if 探索步数 > 0:
        ppo_update(ppo_buffer)
    
    # 如果探索策略在引导步结束后表现良好，减少引导步数
    if exploration_performance > threshold:
        h = max(0, h - Δh)  # Δh递减量
```

#### 3.1.2 课程式引导步数衰减策略

| 训练阶段 | Episode范围 | 引导步数h | 策略 |
|---------|------------|-----------|------|
| 冷启动期 | 0 - 500 | H_max (全episode) | 完全由行为树引导，DRL观察学习 |
| 引导期 | 500 - 2000 | H_max → 0.7×H_max | 逐步减少引导，DRL接管后半段 |
| 过渡期 | 2000 - 4000 | 0.7×H_max → 0.3×H_max | DRL主导后半段决策 |
| 微调期 | 4000 - 6000 | 0.3×H_max → 0.1×H_max | 仅少量引导，DRL近乎自主 |
| 独立期 | 6000 - 10000 | 0 | 完全自主决策 |

**衰减触发条件（自适应）：**
- 当前探索策略的episode平均奖励超过引导策略的N%时触发衰减
- 或固定间隔衰减（每N episode）

#### 3.1.3 PPO算法实现

```python
class PPOAgent:
    """
    用于超视距空战的PPO智能体
    
    网络架构:
        Actor: 状态 → 共享FC(256) → 共享FC(128) → Actor_Head(动作均值μ, 标准差σ)
        Critic: 状态 → 共享FC(256) → 共享FC(128) → Critic_Head(状态价值V)
    
    关键超参数:
        lr_actor: 3e-4
        lr_critic: 1e-3
        gamma: 0.99
        gae_lambda: 0.95
        clip_epsilon: 0.2
        entropy_coef: 0.01
        batch_size: 64
        n_epochs: 10
    """
```

**增强特性：**
- 使用LSTM网络层处理部分可观测性（雷达间歇丢失目标）
- 内在奖励（Intrinsic Reward）：行为树子目标完成情况作为额外奖励信号
- 动作掩码（Action Masking）：禁用无效动作（如无导弹时禁止发射）

---

### 任务3.2：多任务专精DRL智能体训练（第6-14周）

#### 3.2.1 任务定义与设计

| 任务类型 | 目标 | 成功条件 | 训练优先度 |
|---------|------|---------|-----------|
| **超视距打击** (BVR Strike) | 在超视距距离上发现、锁定并击落敌机 | 命中目标 | ★★★★★ |
| **空域巡逻** (Air Patrol) | 按预定航线巡逻，搜索并拦截入侵目标 | 连续巡逻N分钟无渗透 | ★★★★☆ |
| **战术防御** (Tactical Defense) | 保护高价值目标（预警机/加油机）不被攻击 | 高价值目标未被命中 | ★★★★☆ |
| **电子压制** (EW Support) | 对敌方雷达实施电子干扰，掩护友军 | 友军被探测概率降低 | ★★★☆☆ |

#### 3.2.2 分任务训练方案

**任务A：超视距打击智能体**

```
训练场景：
  - 双方初始距离: 80-120km
  - 初始高度: 8000-12000m
  - 态势: 迎头、尾追、侧向（随机）

奖励设计：
  - 命中目标: +200
  - 进入导弹不可逃逸区且态势占优: +50（一次性）
  - 保持在目标攻击区外: +2/step
  - 丢失目标锁定: -10/step
  - 被敌方锁定: -5/step
  - 燃油耗尽: -50
  
训练环境：
  - 并行环境数: 8
  - 每episode最大步数: 600（对应10分钟@1Hz）
  - 总训练步数: 1×10^7
```

**任务B：空域巡逻智能体**

```
训练场景：
  - 巡逻航线: 4个预设导航点组成的矩形航线
  - 敌方: 从随机方位试图渗透的目标
  - 友军: 协同拦截

奖励设计：
  - 拦截敌方目标: +100
  - 保持巡逻航线偏差 < 2km: +0.5/step
  - 敌方渗透成功: -100
  - 燃油低于Bingo: -20
  
训练环境：
  - 并行环境数: 8
  - 每episode最大步数: 1200（对应20分钟@1Hz）
```

**任务C：战术防御智能体**

```
训练场景：
  - 高价值目标（HVA）位于中心区域
  - 僚机在HVA外围巡逻
  - 敌方从多方向接近

奖励设计：
  - HVA存活至episode结束: +150
  - 击落敌方: +100
  - 位于敌方与HVA连线上: +3/step（拦截占位）
  - HVA被命中: -200
```

#### 3.2.3 训练基础设施

**算力需求估算：**
| 项目 | 配置 | 训练时间估计 |
|------|------|-------------|
| 单任务DRL训练 | 1×RTX 4060/4070 或云GPU (T4级别) | ~24-48小时/任务 |
| 并行环境 | 8进程 | - |
| 总计算量 | 4任务 × 10M steps | ~200小时GPU时间 |

**训练监控：**
- TensorBoard 记录：episode奖励、策略loss、价值loss、引导步数衰减曲线
- 定期评估：每500 episode在固定测试态势集上评估
- 模型检查点：每1000 episode保存

**验收标准：**
- 超视距打击智能体：在标准测试态势下命中率 > 60%（对比纯PPO baseline < 30%）
- 空域巡逻智能体：渗透拦截率 > 70%
- 战术防御智能体：HVA存活率 > 80%
- JSRL训练 vs. 纯DRL训练：收敛速度快 3-5倍

---

## 第四阶段：嵌套式混合决策架构与可进化机制设计

**时间段：** 2027年1月 — 2027年4月（12周）  
**负责人：** 李欣洋（主责）、唐煜皓（协责）、徐文强（协责）  
**经费分配：** 会议差旅 ¥1500、算力 ¥2000

---

### 任务4.1：三层调度行为树架构设计与实现（第1-5周）

#### 4.1.1 整体架构

```
┌──────────────────────────────────────────────────────────────┐
│               三层调度行为树 (Scheduling BT)                    │
│                                                               │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │ L1: 监督式指令仲裁子树 (Supervisory Command Arbiter)      │ │
│  │   PrioritySelector: HumanCmd → L2 → L3                  │ │
│  │   ┌──────────────────────────────────────────────────┐  │ │
│  │   │ HasHumanCommand? → ExecuteCommand → Success      │  │ │
│  │   └──────────────────────────────────────────────────┘  │ │
│  └─────────────────────────────────────────────────────────┘ │
│                              │ (无真人指令时放行)               │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │ L2: 安全兜底与应急自保子树 (Safety & Emergency)           │ │
│  │   ┌──────────────────────────────────────────────────┐  │ │
│  │   │ 安全边界校验 → 合规性校验 → 应急自保              │  │ │
│  │   │ (过载/航速/高度)  (武器授权/交战规则)  (导弹规避) │  │ │
│  │   └──────────────────────────────────────────────────┘  │ │
│  └─────────────────────────────────────────────────────────┘ │
│                              │ (安全校验通过后放行)             │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │ L3: DRL智能体调度子树 (DRL Agent Dispatch)               │ │
│  │   PrioritySelector ← 黑板.task_label                     │ │
│  │   ├── strike_agent    (超视距打击智能体)                  │ │
│  │   ├── patrol_agent    (空域巡逻智能体)                    │ │
│  │   ├── defense_agent   (战术防御智能体)                    │ │
│  │   └── default_agent   (默认安全行为树)                    │ │
│  └─────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
```

#### 4.1.2 关键实现细节

**L1：监督式指令仲裁子树**

```python
class CommandType(Enum):
    START_MISSION = "start_mission"        # 启动任务
    ABORT_MISSION = "abort_mission"        # 终止任务
    CHANGE_TASK = "change_task"            # 切换任务类型
    TACTICAL_ADJUST = "tactical_adjust"    # 战术微调
    EMERGENCY_OVERRIDE = "emergency"       # 紧急干预
    FIRE_AUTHORIZATION = "fire_auth"       # 武器授权

class HumanCommand:
    command_type: CommandType
    params: Dict[str, Any]
    timestamp: float
    priority: int  # 1-5，5为最高
    source: str    # "pilot_ui", "ground_control", etc.
```

**L2：安全兜底与应急自保子树**

```python
class SafetyCheckResult:
    passed: bool
    violations: List[str]
    corrected_action: Optional[np.ndarray]  # 修正后的动作
    override_reason: Optional[str]          # 覆盖原因

class SafetyGuard:
    def validate(self, proposed_action, blackboard):
        """对DRL输出动作进行安全性校验"""
        
        # 1. 飞行包线校验
        predicted_state = self._predict_next_state(proposed_action)
        if not within_flight_envelope(predicted_state):
            return SafetyCheckResult(
                passed=False,
                corrected_action=self._envelope_limit_action(proposed_action),
                override_reason="超出飞行包线"
            )
        
        # 2. 交战规则校验
        if proposed_action.fire_missile and not blackboard.get("fire_authorized"):
            return SafetyCheckResult(
                passed=False,
                corrected_action=mask_fire_action(proposed_action),
                override_reason="未授权发射武器"
            )
        
        return SafetyCheckResult(passed=True)
```

**L3：DRL智能体调度子树**

```python
class AgentDispatcher:
    def __init__(self):
        self.agents = {
            "strike": StrikeAgent(),
            "patrol": PatrolAgent(),
            "defense": DefenseAgent(),
            "default": DefaultBehaviorTree(),  # 回退到安全行为树
        }
    
    def select_agent(self, blackboard):
        task_label = blackboard.get("task_label", "default")
        return self.agents.get(task_label, self.agents["default"])
    
    def execute(self, blackboard):
        agent = self.select_agent(blackboard)
        observation = self._build_observation(blackboard)
        action = agent.get_action(observation)
        return self._safety_validate(action, blackboard)
```

---

### 任务4.2：黑板系统设计与实现（第3-7周）

#### 4.2.1 黑板系统架构

```
┌──────────────────────────────────────────────────────────────┐
│                      黑板系统 (Blackboard)                     │
│                                                               │
│  ┌─────────────┐  ┌──────────────┐  ┌─────────────────────┐ │
│  │ 全局态势模块  │  │ 人机指令模块  │  │  行为树节点日志模块   │ │
│  │             │  │              │  │                     │ │
│  │ • 飞机状态   │  │ • 指令缓存    │  │ • 触发节点名        │ │
│  │ • 传感器数据 │  │ • 优先级标签  │  │ • 输入条件          │ │
│  │ • 武器状态   │  │ • 时间戳     │  │ • 输出动作          │ │
│  │ • 敌方态势   │  │ • 执行状态    │  │ • 执行结果          │ │
│  └─────────────┘  └──────────────┘  │ • 时间戳           │ │
│                                      └─────────────────────┘ │
│  ┌─────────────┐  ┌──────────────┐  ┌─────────────────────┐ │
│  │ DRL智能体    │  │ 安全校验模块  │  │  用户反馈日志模块    │ │
│  │ 输入/输出模块 │  │             │  │                     │ │
│  │             │  │ • 校验结果    │  │ • 干预行为          │ │
│  │ • 态势特征   │  │ • 修正动作    │  │ • 评价评分          │ │
│  │ • 动作输出   │  │ • 触发原因    │  │ • 偏好标记          │ │
│  │ • 选择记录   │  │              │  │                     │ │
│  └─────────────┘  └──────────────┘  └─────────────────────┘ │
│                                                               │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │              决策追溯查询接口 (Trace Query API)            │ │
│  │  get_decision_chain(timestamp_range) → 完整决策链         │ │
│  │  get_agent_actions(agent_name) → 智能体历史动作            │ │
│  │  get_safety_overrides() → 安全覆盖记录                    │ │
│  │  export_full_log() → 导出完整日志JSON                    │ │
│  └─────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
```

#### 4.2.2 黑板系统实现

```python
import time
import json
from dataclasses import dataclass, field, asdict
from typing import Dict, List, Any, Optional
from threading import Lock

@dataclass
class BlackboardEntry:
    """黑板条目的基类"""
    timestamp: float
    source: str
    data: Dict[str, Any]

class Blackboard:
    """
    中心化黑板系统
    
    职责：
    1. 维护全局共享数据
    2. 记录全链路决策日志
    3. 支持决策追溯查询
    4. 线程安全
    """
    
    def __init__(self):
        self._lock = Lock()
        
        # 核心数据模块
        self.global_situation: Dict[str, Any] = {}
        self.human_commands: List[BlackboardEntry] = []
        self.node_exec_log: List[BlackboardEntry] = []
        self.agent_io_log: List[BlackboardEntry] = []
        self.safety_check_log: List[BlackboardEntry] = []
        self.user_feedback_log: List[BlackboardEntry] = []
        
        # 当前任务上下文
        self.task_label: str = "default"
        self.fire_authorized: bool = False
        self.mission_active: bool = True
    
    def write(self, module: str, data: Dict[str, Any], source: str):
        """向黑板写入数据"""
        with self._lock:
            entry = BlackboardEntry(
                timestamp=time.time(),
                source=source,
                data=data
            )
            getattr(self, f"{module}_log").append(entry)
    
    def read_situation(self) -> Dict[str, Any]:
        """读取全局态势"""
        return self.global_situation.copy()
    
    def update_situation(self, situation: Dict[str, Any]):
        """更新全局态势"""
        with self._lock:
            self.global_situation.update(situation)
    
    def get_decision_chain(self, start_time: float, end_time: float) -> List[Dict]:
        """追溯指定时间段的完整决策链"""
        with self._lock:
            all_logs = (
                self.human_commands +
                self.node_exec_log +
                self.agent_io_log +
                self.safety_check_log
            )
            filtered = [e for e in all_logs if start_time <= e.timestamp <= end_time]
            filtered.sort(key=lambda e: e.timestamp)
            return [asdict(e) for e in filtered]
    
    def export_full_log(self, filepath: str):
        """导出完整日志"""
        with self._lock:
            full_log = {
                "human_commands": [asdict(e) for e in self.human_commands],
                "node_exec_log": [asdict(e) for e in self.node_exec_log],
                "agent_io_log": [asdict(e) for e in self.agent_io_log],
                "safety_check_log": [asdict(e) for e in self.safety_check_log],
                "user_feedback_log": [asdict(e) for e in self.user_feedback_log],
            }
            with open(filepath, 'w', encoding='utf-8') as f:
                json.dump(full_log, f, indent=2, ensure_ascii=False)
```

---

### 任务4.3：OpenClaw闭环迭代机制（第5-12周）

#### 4.3.1 OpenClaw集成架构

```
┌──────────────────────────────────────────────────────────┐
│                    OpenClaw Framework                      │
│                                                           │
│  ┌──────────┐    ┌──────────┐    ┌───────────────────┐  │
│  │ 飞行员UI  │───▶│ Moltbot  │───▶│   Clawd Scheduler  │  │
│  │ (反馈输入) │    │ 消息总线 │    │   (反馈处理调度器)   │  │
│  └──────────┘    └──────────┘    └────────┬──────────┘  │
│                                           │              │
│                    ┌───────────────────────┤              │
│                    ▼                       ▼              │
│            ┌────────────┐        ┌──────────────┐       │
│            │ DRL微调     │        │ 行为树修正    │       │
│            │ (Fine-tune) │        │ (Rule Update) │       │
│            └────────────┘        └──────────────┘       │
│                    │                       │              │
│                    └───────────┬───────────┘              │
│                                ▼                          │
│                    ┌──────────────────┐                   │
│                    │  策略回滚与验证   │                   │
│                    │  (Rollback Check) │                   │
│                    └────────┬─────────┘                   │
│                             │                             │
│                             ▼                             │
│                    ┌──────────────────┐                   │
│                    │ 新策略部署        │                   │
│                    │ (Deploy Updated)  │                   │
│                    └────────┬─────────┘                   │
│                             │                             │
│                             ▼                             │
│                    ┌──────────────────┐                   │
│                    │ 继续收集反馈      │ ← 正循环          │
│                    └──────────────────┘                   │
└──────────────────────────────────────────────────────────┘
```

#### 4.3.2 反馈信号采集与转化

**反馈类型定义：**

| 反馈类型 | 来源 | 信号格式 | 转化方式 |
|---------|------|---------|---------|
| 直接干预 | 飞行员撤销/覆盖DRL决策 | (state, original_action, corrected_action) | Behavior Cloning训练样本；行为树新增规则分支 |
| 事后评分 | 飞行员对整场空战的评价 | (episode_id, score: 1-5) | 高分episode作为正样本，低分episode作为负样本 |
| 行为偏好标记 | 飞行员对特定行为的标记 | (state, agent_action, preference: good/bad) | 偏好对(pairwise comparison)用于RLHF微调 |
| 建议修正 | 飞行员书面反馈 | (natural_language_feedback) | LLM解析后转化为结构化修正建议 |
| 被动观察 | 飞行员未干预但系统自检异常 | (anomaly_detection_flag) | 自动标记为需审查样本 |

**反馈处理Pipeline：**

```python
class FeedbackPipeline:
    """将多模态反馈转化为优化信号"""
    
    def process(self, raw_feedback: Dict) -> OptimizationSignal:
        signal_type = self._classify(raw_feedback)
        
        if signal_type == "direct_intervention":
            return self._to_bc_sample(raw_feedback)
            
        elif signal_type == "post_hoc_rating":
            return self._to_reward_signal(raw_feedback)
            
        elif signal_type == "behavior_preference":
            return self._to_preference_pair(raw_feedback)
            
        elif signal_type == "suggested_correction":
            return self._parse_llm_feedback(raw_feedback)
    
    def _to_bc_sample(self, feedback):
        """转化为行为克隆样本"""
        return {
            "type": "behavior_cloning",
            "state": feedback["state"],
            "target_action": feedback["corrected_action"],
            "weight": 2.0,  # 直接干预样本权重大
        }
    
    def _to_preference_pair(self, feedback):
        """转化为偏好对"""
        return {
            "type": "pairwise_preference",
            "state": feedback["state"],
            "preferred_action": feedback["good_behavior"],
            "rejected_action": feedback["bad_behavior"],
        }
```

#### 4.3.3 安全训练机制

**策略回滚机制：**
```python
class SafeTrainingWrapper:
    """确保训练安全的包装器"""
    
    def __init__(self):
        self.policy_history = []  # 历史策略版本
        self.performance_threshold = 0.8  # 新策略必须达到旧策略80%性能
    
    def train_and_validate(self, new_data):
        old_policy = copy.deepcopy(current_policy)
        new_policy = fine_tune(old_policy, new_data)
        
        # 在安全验证集上测试
        old_score = evaluate(old_policy, safe_validation_env)
        new_score = evaluate(new_policy, safe_validation_env)
        
        if new_score >= old_score * self.performance_threshold:
            # 新策略不显著劣于旧策略，允许部署
            self._deploy(new_policy)
        else:
            # 回滚到旧策略
            log_warning("新策略性能下降，回滚到旧策略")
            self._deploy(old_policy)
```

**渐进微调策略：**
- 每次微调仅使用最近N条反馈（避免灾难性遗忘）
- 混合原始训练数据（20%）与反馈数据（80%）进行微调
- 行为树规则修正采用"新增规则 + 人工审核"两步法，不直接删除已有规则

---

## 第五阶段：系统验证与效能评估

**时间段：** 2027年4月 — 2027年6月（8周）  
**负责人：** 徐文强（主责）、焦阳（协责）、唐煜皓（协责）  
**经费分配：** 论文出版 ¥2000

---

### 任务5.1：操控功能验证（第1-3周）

#### 5.1.1 验证想定设计

**想定1：有人指令响应测试**
```
场景：僚机正在执行超视距打击任务
事件序列：
  T+0s:  系统以打击DRL智能体自主操作
  T+30s: 飞行员下发"切换至战术防御"指令
  T+30s: 验证调度行为树是否正确切换到防御智能体
  T+60s: 飞行员下发"紧急规避"指令
  T+60s: 验证L1仲裁子树是否立即覆盖当前行为
  
验收标准：
  - 指令响应延迟 < 500ms
  - 切换后行为与指令意图一致
  - 黑板日志完整记录切换全过程
```

**想定2：安全兜底测试**
```
场景：DRL智能体输出危险动作
测试用例：
  1. DRL输出过载G=9（超出平台限制7G）
     → 验证安全子树是否将G值限制到7G
  2. DRL在未获授权时尝试发射武器
     → 验证武器发射被阻止
  3. 敌方导弹逼近（5km内且高速接近）
     → 验证是否自动触发规避机动
  
验收标准：
  - 所有安全违规被成功拦截
  - 安全修正动作物理可行
  - 黑板日志明确记录违规原因与修正动作
```

**想定3：多任务切换测试**
```
场景：连续切换任务类型
事件序列：
  T+0s:  空域巡逻（验证巡逻智能体激活）
  T+30s: 检测到敌方 → 切换至超视距打击
  T+60s: 击落敌机 → 切换回空域巡逻
  T+90s: 预警机受威胁 → 切换至战术防御
  
验收标准：
  - 切换平滑，无停顿/振荡
  - 每次切换后智能体行为符合任务预期
```

---

### 任务5.2：自迭代功能验证（第3-5周）

#### 5.2.1 自迭代验证流程

```
Step 1: 部署初始策略 → 运行操控功能验证 → 收集验证结果
Step 2: 根据验证结果生成改进意见（人工编写 + LLM辅助生成）
Step 3: 将改进意见提交OpenClaw → 系统生成优化方案
Step 4: OpenClaw执行微调/修正 → 生成新策略版本
Step 5: 运行回归测试 → 比较新旧策略性能
Step 6: 评估自迭代效果
```

**迭代轮次设计：** 至少3轮完整迭代

| 迭代轮次 | 关注点 | 预期改进 |
|---------|--------|---------|
| 第1轮 | 安全边界响应 | 降低误报/漏报率 |
| 第2轮 | 多任务切换平滑性 | 减少切换振荡 |
| 第3轮 | 战术决策合理性 | 提升命中率/存活率 |

---

### 任务5.3：效能评估（第5-8周）

#### 5.3.1 评估指标体系

**一级指标（核心指标）：**

| 指标 | 定义 | 计算方式 |
|------|------|---------|
| 任务成功率 | 完成任务目标的episode比例 | 成功episode数 / 总episode数 |
| 敌我交换比 | 击落敌机数 / 我方损失数 | sum(enemy_kills) / sum(own_losses) |
| 平均决策时间 | 从感知到动作输出的平均耗时 | mean(decision_timestamps) |

**二级指标（辅助指标）：**

| 指标 | 定义 |
|------|------|
| 安全违规率 | 每100步中出现安全违规的次数 |
| 指令响应延迟 | 真人指令从下达到执行的时间间隔 |
| 切换振荡次数 | 任务切换后3秒内回切原任务的次数 |
| 首杀时间 | 从episode开始到首次命中目标的平均时间 |
| 武器使用效率 | 命中数 / 武器发射数 |

**三级指标（自迭代评估）：**

| 指标 | 定义 |
|------|------|
| 迭代收敛速度 | N轮迭代后指标提升幅度 |
| 策略稳定性 | 连续迭代中指标方差的倒数 |
| 用户满意度 | 飞行员主观评分（1-5分） |

#### 5.3.2 对比实验设计

**Baseline方案：**

| 方案编号 | 方案描述 | 说明 |
|---------|---------|------|
| Baseline-1 | 纯规则行为树 | 人工手写行为树，无DRL |
| Baseline-2 | 纯PPO（无引导） | 端到端DRL，无行为树引导训练 |
| Baseline-3 | 行为树 + DRL浅层融合（无JSRL） | 训练完的BT和DRL仅在推理时融合 |
| **Ours** | **本项目方案** | **LLM行为树 + JSRL训练 + 三层混合决策 + OpenClaw迭代** |

**实验场景矩阵：**

| 场景 | 初始距离 | 机数对比 | OODA优势 | 测试目标 |
|------|---------|---------|---------|---------|
| 均势对决 | 100km | 1v1 | 无 | 基础能力 |
| 以少敌多 | 80km | 1v2 | 无 | 多目标处理 |
| 以多敌少 | 120km | 2v1 | 我优 | 协同打击 |
| 先敌发现 | 100km | 1v1 | 我优 | OODA优势利用 |
| 后被锁定 | 60km | 1v1 | 敌优 | 劣势反转能力 |
| 带HVA保护 | 90km | 1v1+HVA | 无 | 防御任务 |

每种方案×场景运行100个随机种子episode，统计分析。

**统计方法：**
- 使用Welch's t-test / Mann-Whitney U test检验显著性
- 报告均值 ± 标准差
- 绘制箱线图、雷达图、学习曲线

---

# 第三篇 项目管理与保障

## 六、团队分工与协作机制

### 6.1 详细分工矩阵

| 任务模块 | 李欣洋（负责人） | 唐煜皓 | 徐文强 | 焦阳 |
|---------|:--:|:--:|:--:|:--:|
| Phase 1: 环境搭建 | ○ | ● | ○ | △ |
| Phase 2: 行为树设计 | ● | △ | ○ | ○ |
| Phase 3: DRL训练 | ● | △ | △ | ● |
| Phase 4: 混合架构 | ● | ● | ○ | △ |
| Phase 5: 评估验证 | ○ | ○ | ● | ● |
| 文献调研 | ○ | ○ | ● | △ |
| 报告撰写 | ● | △ | ● | ○ |
| 汇报答辩 | ● | △ | ● | ○ |

图例：● 主责  ○ 参与  △ 辅助

### 6.2 协作机制

- **周例会制度：** 每周五晚进行项目进展汇报（30-60分钟）
- **双周技术研讨会：** 每两周一次技术专题讨论（2小时），由不同成员轮流主持
- **月度导师汇报：** 每月向曾俊杰老师汇报一次进展，提交书面进展报告
- **代码管理：** Git版本控制，分支开发，主分支保护
- **文档管理：** 统一的实验记录模板、会议纪要模板
- **即时通讯：** 微信群/钉钉群日常沟通

---

## 七、经费使用详细计划

| 时间段 | 开支科目 | 金额(元) | 用途说明 |
|--------|---------|---------|---------|
| 2026.6-2026.12（前半阶段） | | **10500** | |
| | 算力租赁 | 4000 | GPU云服务器租用（RL训练用） |
| | 材料费 | 2000 | 开发用外设、耗材 |
| | 会议差旅 | 1500 | 学术交流、调研 |
| | 文献检索 | 1000 | 专业书籍、数据库检索 |
| | 仪器设备 | 2000 | 开发测试设备 |
| 2027.1-2027.6（后半阶段） | | **9500** | |
| | 算力租赁 | 4000 | 训练、验证、迭代用计算资源 |
| | 材料费 | 2000 | 实验耗材、演示用设备 |
| | 会议差旅 | 1500 | 成果交流、结题汇报 |
| | 论文出版 | 2000 | 专利申请、论文发表费用 |

---

## 八、风险分析与应对措施

| 风险类别 | 风险描述 | 发生概率 | 影响程度 | 应对措施 |
|---------|---------|:--:|:--:|---------|
| 技术风险 | 选型机型的性能约束与老旧平台实际性能存在偏差 | 低 | 低 | 选用JSBSim成熟机型，物理参数已有验证；性能约束为软件层限定，可灵活调整；约束参数标注为"参考配置"，不声称精确对应某一机型 |
| 技术风险 | PPO智能体训练不收敛 | 中 | 高 | 加入JSRL引导后预期大幅改善；备选使用SAC/SQL等off-policy算法 |
| 技术风险 | LLM生成行为树质量不稳定 | 中 | 高 | 多轮迭代 + 人工审核 + 仿真验证的三重质量保障；复杂的战术子树可退化为人工手写 |
| 技术风险 | OpenClaw框架兼容性问题 | 高 | 中 | 预备自研简化版反馈收集与策略更新脚本作为Plan B |
| 时间风险 | 某阶段延迟导致后续阶段压缩 | 中 | 中 | 各阶段预留2周缓冲时间；按优先级裁剪非核心功能 |
| 人员风险 | 团队成员学业冲突 | 中 | 中 | 关键模块每人至少一名备份负责人；利用寒暑假集中攻关 |
| 资源风险 | GPU算力不足 | 低 | 中 | 预算已预留充足算力经费；优先使用实验室/学校计算资源 |

---

## 九、预期成果与交付物清单

| 编号 | 交付物 | 形式 | 预期完成时间 | 责任人 |
|:----:|--------|------|-------------|--------|
| D1 | 高保真超视距空战仿真环境 | 源码 + 使用文档 | 2026.8 | 唐煜皓 |
| D2 | 适配机型选型报告与性能约束配置 | 选型对比文档 + 约束配置文件 + 验证日志 | 2026.7 | 唐煜皓 |
| D3 | LLM行为树生成器-测试器框架 | 源码 + Prompt库 | 2026.10 | 李欣洋 |
| D4 | 超视距空战引导策略行为树 | 行为树定义 + 测试报告 | 2026.10 | 李欣洋 |
| D5 | JSRL训练框架 | 源码 + 训练配置 | 2026.11 | 李欣洋 |
| D6 | 多任务DRL智能体模型（≥3个） | 模型权重 + 训练日志 | 2027.1 | 李欣洋/焦阳 |
| D7 | 三层调度行为树 | 源码 + 单元测试 | 2027.2 | 李欣洋/唐煜皓 |
| D8 | 黑板系统 | 源码 + API文档 | 2027.3 | 唐煜皓 |
| D9 | OpenClaw自迭代模块 | 源码 + 集成文档 | 2027.3 | 李欣洋 |
| D10 | 完整混合决策系统 | 集成源码 + 部署指南 | 2027.4 | 全员 |
| D11 | 操控/自迭代/效能验证报告 | 实验报告 + 数据 | 2027.5 | 徐文强/焦阳 |
| D12 | 项目研究报告 | Word/PDF文档 | 2027.6 | 徐文强/李欣洋 |
| D13 | 学术论文 | 投稿论文 | 2027.6 | 李欣洋/徐文强 |
| D14 | 专利申请材料 | 专利文档 | 2027.6 | 李欣洋 |

---

## 十、关键里程碑节点

```
2026.06.01 ━━━ 项目启动 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
2026.06.15 ━━ 完成数据收集与整理                            ┃ Phase 1
2026.07.15 ━━ JSBSim环境搭建完成，功能验证通过              ┃ (8周)
2026.08.01 ━━━ 行为树生成器-测试器框架搭建                  ┃ Phase 2
2026.08.30 ━━ LLM行为树生成流程跑通                        ┃ (12周)
2026.09.30 ━━ 引导策略行为树完成仿真验证                    ┃
2026.10.15 ━━━ JSRL训练框架实现完成                         ┃ Phase 3
2026.11.15 ━━ 首个DRL智能体训练完成                         ┃ (14周)
2026.12.31 ━━ 全部DRL智能体训练完成                         ┃
2027.01.15 ━━━ 调度行为树架构完成                           ┃ Phase 4
2027.02.15 ━━ 黑板系统实现完成                              ┃ (12周)
2027.03.15 ━━ OpenClaw集成完成                              ┃
2027.04.15 ━━━ 操控验证完成                                 ┃ Phase 5
2027.05.15 ━━ 自迭代验证与效能评估完成                      ┃ (8周)
2027.06.01 ━━ 项目结题，交付所有成果                        ┃
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
```

---

# 第四篇 补充技术细节

## 十一、关键算法伪代码

### 11.1 JSRL双策略课程式训练

```
Algorithm: JSRL Train with Behavior Tree Guide

Input:
  env: 空战环境
  bt_policy: 行为树引导策略
  ppo_agent: PPO智能体
  total_episodes: 总训练episode数

Output:
  trained_ppo_agent

function train():
    guide_steps = env.max_steps          # 初始引导步数 = 全长
    ppo_buffer = ReplayBuffer()
    
    for episode in range(total_episodes):
        obs = env.reset()
        done = False
        episode_reward = 0
        guided_count = 0
        
        for step in range(env.max_steps):
            if step < guide_steps:
                action = bt_policy.get_action(obs)
                guided_count += 1
            else:
                action, log_prob, value = ppo_agent.get_action(obs)
                ppo_buffer.add(obs, action, log_prob, value)
            
            next_obs, reward, done, info = env.step(action)
            
            # 行为树子目标完成给予内在奖励
            if step < guide_steps:
                subgoal_reward = bt_policy.get_subgoal_reward(info)
                if guided_reward_buffer:
                    # 将子目标奖励传播给最近的探索步骤
                    ppo_buffer.reward_bonus.append(subgoal_reward)
            
            obs = next_obs
            episode_reward += reward
            
            if done: break
        
        # PPO更新（仅在探索步数足够时）
        if ppo_buffer.size() > batch_size:
            ppo_agent.update(ppo_buffer)
            ppo_buffer.clear()
        
        # 课程式衰减引导步数
        if episode % eval_interval == 0:
            eval_score = evaluate(ppo_agent)
            if eval_score > bt_policy.eval_score * decay_threshold:
                guide_steps = max(0, guide_steps - decay_amount)
```

### 11.2 三层混合决策主循环

```
Algorithm: Hybrid Decision Loop

function hybrid_decision_loop():
    blackboard = Blackboard()
    schedule_bt = ScheduleBehaviorTree()  # 含L1/L2/L3
    
    env = BVRCombatEnv()
    obs = env.reset()
    done = False
    
    while not done:
        # Step 1: 更新黑板 - 从环境读取全局态势
        blackboard.update_situation(obs)
        
        # Step 2: 读取真人指令（异步接口）
        human_cmd = human_interface.poll_command()
        if human_cmd is not None:
            blackboard.write("human_commands", human_cmd, "human_interface")
        
        # Step 3: 三层调度行为树决策
        action = schedule_bt.tick(blackboard)
        
        # Step 4: 执行动作
        next_obs, reward, done, info = env.step(action)
        
        # Step 5: 收集用户反馈（异步）
        feedback = user_feedback_interface.poll_feedback()
        if feedback is not None:
            blackboard.write("user_feedback_log", feedback, "user_interface")
        
        obs = next_obs
    
    # Episode结束：导出日志
    blackboard.export_full_log(f"episode_{episode_id}_log.json")
    
    # 定期触发OpenClaw自迭代
    if should_iterate():
        openclaw.submit_feedback(blackboard.user_feedback_log)
```

---

## 十二、开发环境与技术栈

| 类别 | 技术选型 | 说明 |
|------|---------|------|
| 仿真引擎 | JSBSim v1.2.4 | 已安装，C++核心 + Python桥接 |
| 深度学习框架 | PyTorch 2.x | 团队已掌握 |
| 强化学习算法 | PPO (主) / SAC (备) | PPO在连续控制中较为稳定 |
| 大语言模型 | GPT-4o / DeepSeek / 本地部署开源模型 | 用于行为树生成，根据预算和可行性选择 |
| 行为树引擎 | 自研Python BT Engine | 轻量化，便于与DRL框架深度集成 |
| 可视化 | Matplotlib + Folium / FlightGear | 2D战术显示为主 |
| 版本控制 | Git + GitHub | 代码管理 |
| 实验管理 | TensorBoard + Weights & Biases | 训练监控 |
| 文档协作 | 腾讯文档 / Notion / GitHub Wiki | 团队文档 |
| 算力平台 | AutoDL / 学校集群 / 本地GPU | GPU训练 |

---

## 十三、参考文献补充（现有基础）

除申请书中已引用的[1]-[33]外，项目研究中将重点参考：

1. JSBSim官方参考手册 (https://jsbsim-team.github.io/jsbsim-reference-manual/)
2. JSBSim C++ API文档 — 用于仿真环境深度定制
3. Schulman et al. "Proximal Policy Optimization Algorithms" (arXiv:1707.06347) — PPO算法基础
4. Uchendu et al. "Jump-Start Reinforcement Learning" (ICML 2023) — JSRL框架理论基础
5. Colledanchise & Ögren. "Behavior Trees in Robotics and AI: An Introduction" (CRC Press, 2018) — 行为树理论
6. OpenClaw官方文档 (https://docs.openclaw.ai/)
7. Aeromatic++ 使用指南 — 辅助飞行器模型快速生成
8. FlightGear输入/输出协议 — 可视化集成参考

---

# 附录

## 附录A：缩略语对照表

| 缩略语 | 全称 | 中文 |
|--------|------|------|
| BVR | Beyond Visual Range | 超视距 |
| DRL | Deep Reinforcement Learning | 深度强化学习 |
| PPO | Proximal Policy Optimization | 近端策略优化 |
| JSRL | Jump-Start Reinforcement Learning | 快速启动强化学习 |
| LLM | Large Language Model | 大语言模型 |
| BT | Behavior Tree | 行为树 |
| OODA | Observation-Orientation-Decision-Action | 观察-判断-决策-行动 |
| MCP | Model Context Protocol | 模型上下文协议 |
| LAE | Launch Acceptability Envelope | 可发射区域（攻击区） |
| NEZ | No-Escape Zone | 不可逃逸区 |
| HVA | High Value Asset | 高价值目标 |
| FCS | Flight Control System | 飞行控制系统 |
| RMS | Root Mean Square | 均方根 |
| API | Application Programming Interface | 应用程序接口 |
| DOF | Degree of Freedom | 自由度 |

## 附录B：团队成员信息

| 姓名 | 学号 | 专业班级 | 职责 |
|------|------|---------|------|
| 李欣洋 | 202406138018 | 目标工程 | 项目负责人：算法开发、系统架构、行为树设计、DRL训练 |
| 唐煜皓 | 202306144024 | 仿真工程 | 技术骨干：仿真环境搭建、接口开发、黑板系统实现 |
| 徐文强 | 202506141034 | 目标保障 | 研究支持：文献整理、数据管理、验证评估、报告撰写 |
| 焦阳 | 202506139004 | 仿真工程 | 技术辅助：模型调参、效能评估、辅助测试 |

---

*本文档为研究计划与实施方案的详细版本，涵盖技术路线、实施步骤、验收标准、风险应对和资源安排。请项目组成员仔细审阅，如有修改意见请在3个工作日内反馈给项目负责人。*

*待审核确认后，本方案将作为项目执行的基准文件，各阶段进展将对照本方案进行跟踪检查。*
